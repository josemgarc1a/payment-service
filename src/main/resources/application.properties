spring.application.name=payments-service

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update

# Serialize Instant as ISO-8601 strings, not epoch milliseconds
spring.jackson.serialization.write-dates-as-timestamps=false
